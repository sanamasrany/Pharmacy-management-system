import 'package:flutter/material.dart';

class InformationMedicine extends StatelessWidget {
  const InformationMedicine({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
